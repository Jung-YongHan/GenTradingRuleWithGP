# GenTradingRuleWithGP — 실행/벤치마크 의존성
#
# 주의: 기존 requirements.txt 는 conda 의 `@ file:///...` 로컬 경로가 섞여 있어
# 타 환경에서 `pip install` 이 불가능했다(재현성 문제). 표준 핀으로 정리했다.
# Python 3.10+ 권장(numpy 2.3.x 가 3.10 이상 요구). 본 벤치마크는 3.11 에서 측정.

deap==1.4.3
numpy==2.3.3
pandas==2.3.2
plotly==6.3.0
matplotlib==3.11.0
pydot==4.0.1

# 테스트(선택)
pytest==9.0.3
